# Core
pandas>=2.0,<3.0
numpy>=1.24,<2.0
pydantic>=2.0,<3.0

# LLM / AI
openai>=1.0,<2.0

# Document generation
python-docx>=1.0,<2.0
docxtpl>=0.16,<1.0
openpyxl>=3.0,<4.0
xlrd>=2.0,<3.0

# PDF generation (fallback chain: LibreOffice → docx2pdf → fpdf2)
docx2pdf>=0.1,<1.0
fpdf2>=2.8,<3.0

# Document AI (optional — graceful fallback if missing)
docling>=2.0,<3.0
